#!/usr/bin/env bash
# macOS post-install hook. Runs after `pacaptr -S` completes.
# Add commands here for things pacaptr can't express (defaults write, etc.).
set -euo pipefail
# Intentionally empty — add hooks as you need them.
